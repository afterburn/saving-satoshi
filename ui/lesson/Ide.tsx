'use client'

import clsx from 'clsx'
import React, { useState, useEffect, useRef } from 'react'
import { StatusBar, useLessonContext } from 'ui'
import { LessonView } from 'types'

export default function Ide({ success, lines, next, onChange }) {
  return (
    <div>
      <input></input>
    </div>
  )
}
